{
  "name": "life-strategy-library",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix sites-app run tauri:dev",
    "build": "npm --prefix sites-app run tauri:build",
    "test": "npm --prefix sites-app test",
    "check": "npm --prefix sites-app run check"
  }
}
